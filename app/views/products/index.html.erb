<h1>List of Products</h1>
<%= link_to "Sell Your Product", new_product_path %>

<div class=listing>
<ul>
  <% @products.each do |product| %>
  <li><%= link_to image_tag(product.image, style: "width: 100px;"), product %>
  <%= link_to product.name, product %></li>
  <% end %>
  </ul>
</div>
